# Production Environment Variables for Cloudflare Workers Deployment

# API Backend URL (used by server-side proxy route)
API_BACKEND_URL=https://plcassistantbackend.gabrypiritore.workers.dev

# Client no longer needs this — all API calls go through the proxy
NEXT_PUBLIC_API_URL=
