# Local Development Environment Variables

# API Backend URL (used by server-side proxy route)
API_BACKEND_URL=http://localhost:3001

# Client no longer needs this — all API calls go through the proxy
NEXT_PUBLIC_API_URL=
