/**
 * DEPRECATED — API proxy is now handled by middleware.ts via NextResponse.rewrite()
 * and by next.config.js rewrites. This file is intentionally empty to avoid
 * route conflicts with the middleware-based proxy.
 *
 * DO NOT add route handlers here.
 */
export {};
